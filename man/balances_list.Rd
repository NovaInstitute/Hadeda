% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balances_list.R
\name{balances_list}
\alias{balances_list}
\title{List account balances at a consensus timestamp}
\usage{
balances_list(
  config,
  limit = NULL,
  account_id = NULL,
  balance = NULL,
  balance_min = NULL,
  balance_max = NULL,
  public_key = NULL,
  timestamp = NULL,
  order = c("desc", "asc"),
  .transport = NULL
)
}
\arguments{
\item{config}{A configuration list created by \code{\link[=hadeda_config]{hadeda_config()}}.}

\item{limit}{Optional page size for the Mirror Node request.}

\item{account_id}{Optional account ID filter.}

\item{balance}{Optional balance filter expressed in tinybars for equality
matching.}

\item{balance_min}{Optional minimum balance filter (in tinybars).}

\item{balance_max}{Optional maximum balance filter (in tinybars).}

\item{public_key}{Optional public key filter.}

\item{timestamp}{Optional consensus timestamp filter. Accepts comparison
operators such as \code{"gt:"}, \code{"lt:"}, and \code{"eq:"} prefixes supported by the
Mirror Node API.}

\item{order}{Result ordering, either "desc" or "asc".}

\item{.transport}{Optional transport override ("rest" or "grpc").}
}
\value{
A tibble containing account balances at the requested timestamp.
}
\description{
Retrieve aggregated account balances from the Hedera Mirror Node REST API.
The endpoint returns a snapshot of all accounts at a consensus timestamp and
supports filtering by account ID, balance thresholds, public keys, and
ordering options.
}
\examples{
mirror <- hadeda_config(network = "testnet")
\dontrun{
  balances_list(
    mirror,
    limit = 25,
    timestamp = "lt:1700000000.000000000",
    balance_min = 1e8
  )
}

}

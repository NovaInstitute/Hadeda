% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract_service.R
\name{contract_call_local}
\alias{contract_call_local}
\title{Execute a local smart contract query}
\usage{
contract_call_local(
  config,
  contract_id,
  function_signature = NULL,
  parameters = list(),
  call_data = NULL,
  gas = NULL,
  sender_account_id = NULL,
  max_result_size = NULL,
  .transport = NULL
)
}
\arguments{
\item{config}{Configuration created by \code{\link[=hadeda_config]{hadeda_config()}}.}

\item{contract_id}{Target contract identifier.}

\item{function_signature}{Canonical function signature used when encoding
parameters (for example \code{"transfer(address,uint256)"}).}

\item{parameters}{Optional list or atomic vector of parameters supplied when
\code{function_signature} is provided.}

\item{call_data}{Optional hexadecimal string containing pre-encoded call
data. When supplied \code{function_signature} and \code{parameters} are ignored.}

\item{gas}{Optional gas limit forwarded to the gRPC handler.}

\item{sender_account_id}{Optional account submitting the transaction.}

\item{max_result_size}{Optional ceiling for the response payload size accepted
by the handler.}

\item{.transport}{Optional transport override. Only the gRPC transport is
supported.}
}
\value{
A tibble with the call metadata and \code{ContractFunctionResult} fields.
}
\description{
Invoke the SmartContractService \code{contractCallLocalMethod} RPC which executes a
contract function without submitting a transaction to the network. Callers can
provide either ABI parameters via \code{function_signature}/\code{parameters} or raw
pre-encoded \code{call_data}.
}

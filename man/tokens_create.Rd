% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tokens_create.R
\name{tokens_create}
\alias{tokens_create}
\title{Create a new Hedera token}
\usage{
tokens_create(
  config,
  name,
  symbol,
  treasury_account_id,
  initial_supply = NULL,
  decimals = NULL,
  token_type = c("fungible_common", "non_fungible_unique"),
  memo = NULL,
  freeze_default = NULL,
  max_fee = NULL,
  wait_for_receipt = TRUE,
  .transport = NULL
)
}
\arguments{
\item{config}{A configuration list from \code{\link[=hadeda_config]{hadeda_config()}}.}

\item{name}{The token name.}

\item{symbol}{The token symbol.}

\item{treasury_account_id}{The treasury account responsible for the token supply.}

\item{initial_supply}{Optional initial supply for fungible tokens.}

\item{decimals}{Optional number of decimals for fungible tokens.}

\item{token_type}{Token type, either "fungible_common" or "non_fungible_unique".}

\item{memo}{Optional token memo.}

\item{freeze_default}{Optional logical indicating the default freeze state.}

\item{max_fee}{Optional fee ceiling expressed in tinybars when using the
gRPC transport.}

\item{wait_for_receipt}{Logical indicating whether the gRPC handler should
wait for a transaction receipt acknowledgement.}

\item{.transport}{Optional transport override.}
}
\value{
A tibble containing the newly created token identifier and
transaction metadata.
}
\description{
Submit a token creation request over the REST transport or issue a
TokenService \code{createToken} transaction via gRPC.
}
\examples{
hashio <- hadeda_config(
  network = "testnet",
  rest = list(
    base_url = "https://testnet.hashio.io/api/v1",
    headers = list(`X-API-Key` = Sys.getenv("HASHIO_API_KEY"))
  ),
  default_transport = "rest"
)
\dontrun{
treasury_id <- "0.0.5005"
token <- tokens_create(
  hashio,
  name = "Hadeda Example",
  symbol = "HADEDA",
  treasury_account_id = treasury_id
)
token$token_id
}

}

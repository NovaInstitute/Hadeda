% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hadeda_config.R
\name{hadeda_config}
\alias{hadeda_config}
\title{Build a Hadeda transport configuration}
\usage{
hadeda_config(
  network = c("testnet", "mainnet", "previewnet", "localhost"),
  rest = NULL,
  grpc = NULL,
  default_transport = NULL
)
}
\arguments{
\item{network}{One of "mainnet", "testnet", "previewnet", or "localhost".
The value seeds sensible defaults for both the REST and gRPC transports.}

\item{rest}{Optional list overriding the default REST configuration.}

\item{grpc}{Optional list overriding the default gRPC configuration.}

\item{default_transport}{Which transport to prefer when not explicitly specified.}
}
\value{
A named list with entries \code{network}, \code{rest}, \code{grpc}, and \code{default_transport}.
}
\description{
Construct transport settings for communicating with Hedera services.
}
\examples{
config <- hadeda_config(network = "testnet")
config$rest$base_url

custom_config <- hadeda_config(
  network = "testnet",
  rest = list(rate_limit = list(requests_per_second = 5)),
  default_transport = "rest"
)
custom_config$rest$rate_limit

}

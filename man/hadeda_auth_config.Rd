% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth_config.R
\name{hadeda_auth_config}
\alias{hadeda_auth_config}
\title{Configure Hadeda with operator credentials}
\usage{
hadeda_auth_config(
  network = c("testnet", "mainnet", "previewnet", "localhost"),
  operator_id = Sys.getenv("HADEDA_OPERATOR_ID"),
  operator_key = Sys.getenv("HADEDA_OPERATOR_KEY"),
  rest = NULL,
  grpc = NULL,
  default_transport = "grpc",
  signer = NULL
)
}
\arguments{
\item{network}{Hedera network to target (\code{"testnet"}, \code{"mainnet"},
\code{"previewnet"}, or \code{"localhost"}). Defaults to \code{"testnet"}.}

\item{operator_id}{Operator account identifier. Defaults to
\code{Sys.getenv("HADEDA_OPERATOR_ID")}.}

\item{operator_key}{Operator private key (PEM). Defaults to
\code{Sys.getenv("HADEDA_OPERATOR_KEY")}.}

\item{rest}{Optional overrides for the REST configuration (passed to
\code{\link[=hadeda_config]{hadeda_config()}}).}

\item{grpc}{Optional overrides for the gRPC configuration. Values supplied
here take precedence over the defaults injected by this helper, so you can
provide channel factories or pre-built handlers.}

\item{default_transport}{Desired default transport. Defaults to \code{"grpc"},
ensuring that gRPC helpers are available without further changes.}

\item{signer}{Optional signing function. When \code{NULL}, the helper attempts to
construct one from \code{operator_key} using \code{\link[=hadeda_grpc_ed25519_signer]{hadeda_grpc_ed25519_signer()}}. Set
this to \code{FALSE} to skip automatic signer creation.}
}
\value{
A configuration list compatible with \code{hadeda} helpers.
}
\description{
This helper wraps \code{\link[=hadeda_config]{hadeda_config()}} and injects operator credentials into the
gRPC transport. It validates the supplied account identifier and private key
(defaulting to \code{HADEDA_OPERATOR_ID} / \code{HADEDA_OPERATOR_KEY}) and, when
possible, prepares a signing function using \code{\link[=hadeda_grpc_ed25519_signer]{hadeda_grpc_ed25519_signer()}}.
}
\examples{
\dontrun{
  config <- hadeda_auth_config(network = "testnet")
}

}
